# Module 8 — Hands-on labs

Generated from `docs/MODULE8.md` for slides, PDF, and video.
Run commands from the **course repository root** unless noted.

**Before you start:** Read `docs/MODULE8.md` → **How to Learn This Module**, then work through each lab in order.

## 1. One-page cheat sheet (version timeline, subsets, migration, version selection) (`quick_ref/`)

**Folder:** `module8/examples/quick_ref/`

**Run:**

```bash
cd module8/examples
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/quick_ref/`.

## 2. Version timeline (standard, year, role, module) (`version_timeline/`)

**Folder:** `module8/examples/version_timeline/`

**Run:**

```bash
cd module8/examples/version_timeline
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/version_timeline/`.

## 3. Verilog-only vs SystemVerilog design subset (ports, types, comb/seq, when to use) (`design_subset/`)

**Folder:** `module8/examples/design_subset/`

**Run:**

```bash
cd module8/examples/design_subset
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/design_subset/`.

## 4. Tool support summary (simulators, synthesis, lint, formal; Icarus, Verilator, commercial) (`tool_support/`)

**Folder:** `module8/examples/tool_support/`

**Run:**

```bash
cd module8/examples/tool_support
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/tool_support/`.

## 5. "Which standard has X?" for key constructs (logic, always_comb, interface, etc.) (`construct_lookup/`)

**Folder:** `module8/examples/construct_lookup/`

**Run:**

```bash
cd module8/examples/construct_lookup
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/construct_lookup/`.

## 6. Course map (Modules 1–8 with title and content summary) (`course_map/`)

**Folder:** `module8/examples/course_map/`

**Run:**

```bash
cd module8/examples/course_map
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/course_map/`.

## 7. Migration steps (1995→2001, 1364→1800, 1800→1800) and rules of thumb (`migration_steps/`)

**Folder:** `module8/examples/migration_steps/`

**Run:**

```bash
cd module8/examples/migration_steps
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/migration_steps/`.

## 8. Synthesizable do/avoid (assign, always_comb/@*, single driver; avoid delays, defparam, latches) (`synthesizable_subset/`)

**Folder:** `module8/examples/synthesizable_subset/`

**Run:**

```bash
cd module8/examples/synthesizable_subset
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/synthesizable_subset/`.

## 9. Version selection (match tools/IP, state one revision and subset, document) (`version_selection/`)

**Folder:** `module8/examples/version_selection/`

**Run:**

```bash
cd module8/examples/version_selection
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/version_selection/`.

## 10. Learning path (1→…→6, then 7; use 8 as reference) and where to go next (`learning_path/`)

**Folder:** `module8/examples/learning_path/`

**Run:**

```bash
cd module8/examples/learning_path
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/learning_path/`.

## 11. Common pitfalls quick reference (construct table, subset rule, regression, tool support, Module 8 as reference) (`pitfalls/`)

**Folder:** `module8/examples/pitfalls/`

**Run:**

```bash
cd module8/examples/pitfalls
make clean && make run
```

**You should see:** Simulation completes without errors; check `$display` output or PASS messages in the log.

**Go deeper:** Full context in `docs/MODULE8.md` and RTL under `module8/examples/pitfalls/`.
