# Module 7 Examples

Hands-on **Icarus Verilog** demos for Module 7. Run from the course repo root unless noted.

---

## 7. Migration 1364→1800 (`migration/`)

Build and simulate: `module7/examples/migration/`.

**Try these** (from repo root):

```bash
cd module7/examples/migration
make clean && make run
```

---

## 8. Migration 1995→2001 (`migration_1995_to_2001/`)

Build and simulate: `module7/examples/migration_1995_to_2001/`.

**Try these** (from repo root):

```bash
cd module7/examples/migration_1995_to_2001
make clean && make run
```

---

## 9. Case Versions (`case_versions/`)

Build and simulate: `module7/examples/case_versions/`.

**Try these** (from repo root):

```bash
cd module7/examples/case_versions
make clean && make run
```

---

## 10. Version Table (`version_table/`)

Build and simulate: `module7/examples/version_table/`.

**Try these** (from repo root):

```bash
cd module7/examples/version_table
make clean && make run
```

---

## 11. Version Selection (`version_selection/`)

Build and simulate: `module7/examples/version_selection/`.

**Try these** (from repo root):

```bash
cd module7/examples/version_selection
make clean && make run
```

---

## 14. Migration Checklist (`migration_checklist/`)

Build and simulate: `module7/examples/migration_checklist/`.

**Try these** (from repo root):

```bash
cd module7/examples/migration_checklist
make clean && make run
```

---

## 15. Incremental Migration (`incremental_migration/`)

Build and simulate: `module7/examples/incremental_migration/`.

**Try these** (from repo root):

```bash
cd module7/examples/incremental_migration
make clean && make run
```

---

## 16. Port Style Compare (`port_style_compare/`)

Build and simulate: `module7/examples/port_style_compare/`.

**Try these** (from repo root):

```bash
cd module7/examples/port_style_compare
make clean && make run
```

---

## 17. No defparam (`no_defparam/`)

Build and simulate: `module7/examples/no_defparam/`.

**Try these** (from repo root):

```bash
cd module7/examples/no_defparam
make clean && make run
```

---
