        # Narration script — Module 4: IEEE 1800-2005 (SystemVerilog Design Subset)

        **Target length:** ~40 minutes (93 slides; auto-generated — edit per slide as needed)

        ## Timing table

        | Slide | Section | Duration | Narration |
|-------|---------|----------|-----------|
| 1 | Module 4 | 0:25 | Welcome to module 4, IEEE 1800-2005 (SystemVerilog Design Subset). In this module you will master the first systemverilog standard (ieee 1800-2005) for rtl design: logic types, always_comb/always_ff, interfaces, packages, and unique/priority case.. |
| 2 | Learning objectives | 0:16 | Here is what you will learn in this module. Master the first SystemVerilog standard (IEEE 1800-2005) for RTL design: logic types, always_comb/always_ff, interfaces, packages, and... |
| 3 | Prerequisites | 0:16 | Before you start, make sure you have these prerequisites. See module README |
| 4 | Learning path | 0:22 | Learning path. Master the first SystemVerilog standard (IEEE 1800-2005) for RTL design: logic types, always_comb/always_ff, interfaces, packages, and... |
| 5 | Overview | 0:16 | Overview. This module covers IEEE Std 1800-2005, the first SystemVerilog standard. It extends Verilog (IEEE 1364-2005) with design and... |
| 6 | How to learn this module | 0:08 | Next section: How to learn this module. |
| 7 | Suggested learning path (1/2) | 0:32 | Follow this learning path. Read the guides before running the labs. Skim this document — Goal, Overview, and Topics Covered set the IEEE scope for Module 4. Study design architecture — See how DUTs, examples, and testbenches fit together under module4/. Work through labs in order — Open module4/EXAMPLES.md and run each make clean && make run from the repo root. Run the full module script —... |
| 8 | Suggested learning path (2/2) | 0:20 | Follow this learning path. Read the guides before running the labs. Review Common Pitfalls — Can you explain each mistake and the fix? Self-check — Use module4/CHECKLIST.md before starting the next module. From docs/MODULE4.md — read guides before running demos. |
| 9 | Design architecture | 0:08 | Next section: Design architecture. |
| 10 | 1. Repository layout (module4/) (1/2) | 0:38 | 1. Repository layout (module4/) (1/2). First SystemVerilog module — .sv sources and design subset focus logic replaces wire/reg for single-driver RTL in most examples always_comb / always_ff / always_latch — explicit procedural intent interfaces and packages — connectivity and shared types Refer to the diagram on the right. |
| 11 | 1. Repository layout (module4/) (2/2) | 0:16 | 1. Repository layout (module4/) (2/2). scripts/module4.sh — requires simulator with SV design subset |
| 12 | 2. SystemVerilog design layering | 0:38 | 2. SystemVerilog design layering. Packages hold typedefs, constants, functions — import in modules Interfaces bundle signals; modports define direction per connection always_comb infers sensitivity; always_ff targets flip-flops unique/priority case — standard semantics for full case checking Refer to the diagram on the right. |
| 13 | 3. File and compilation units | 0:34 | 3. File and compilation units. .sv extension signals SystemVerilog to the tool Package before import; interface before modport connection See verification_architecture.png — TB still drives DUT via ports or interface Refer to the diagram on the right. |
| 14 | RTL block diagram (reference) | 0:22 | RTL block diagram (reference). Module 4: DUT hierarchy and signal flow. |
| 15 | Verification / testbench diagram (reference) | 0:22 | Verification / testbench diagram (reference). Module 4: stimulus, observation, and checking. |
| 16 | logic ports and always_comb | 0:28 | logic ports and always_comb. Review the code on screen and match it to files in the repository. |
| 17 | Interface + modport connection | 0:28 | Interface + modport connection. Review the code on screen and match it to files in the repository. |
| 18 | Execution & simulation flow | 0:08 | Next section: Execution & simulation flow. |
| 19 | How the example runs (toolchain) | 0:32 | How the example runs (toolchain). Match each bullet to files in the repository. Makefile: Verilator + UVM_HOME compile RTL, interface, and test_*.sv UVM phases: build → connect → run_test → report (same pattern as Module 2) Sequence → driver → DUT → monitor → scoreboard (read SCOREBOARD at end) Typical command: make SIM=verilator TEST=test_* (see module4 demo slide) Loopback / hooks connect... |
| 20 | Key files to study | 0:08 | Next section: Key files to study. |
| 21 | Open these in the repo | 0:32 | Open these in the repo. module4/examples/logic_ports/logic_ports.sv module4/examples/procedural/always_comb_ff.sv module4/examples/interfaces/bus_if_example.sv module4/examples/packages/pkg_util.sv scripts/module4.sh Trace while running module4/EXAMPLES.md labs. |
| 22 | Verification & testing methods | 0:08 | Next section: Verification & testing methods. |
| 23 | 1. Running SystemVerilog labs | 0:34 | 1. Running SystemVerilog labs. cd module4/examples/<name> && make clean && make run ./scripts/module4.sh — full module; some labs need full SV support iverilog supports subset; check build.log if a feature is unsupported Refer to the diagram on the right. |
| 24 | 2. What to check in simulation | 0:34 | 2. What to check in simulation. logic ports compile without separate wire/reg declarations always_comb blocks reject clock/reset inside (tool error = correct) Interface examples connect via modport, not dozens of scalar ports Refer to the diagram on the right. |
| 25 | Package import pattern | 0:28 | Package import pattern. Review the code on screen and match it to files in the repository. |
| 26 | Syllabus topics | 0:08 | Next section: Syllabus topics. |
| 27 | Protocol & design details | 0:08 | Next section: Protocol & design details. |
| 28 | Detail: What 1800-2005 Adds Over 1364-2005 (Design Subset) ( | 0:28 | Protocol and design detail: What 1800-2005 Adds Over 1364-2005 (Design Subset) (1). logic: Single type for single-driver nets/variables (replaces wire/reg in most RTL) 2-state types: bit, int, byte, etc. (mainly for testbenches; some RTL use) always_comb, always_ff, always_latch: Explicit procedural blocks with tool checks Interfaces and modports: Bundle signals and define direction per... |
| 29 | Detail: What 1800-2005 Adds Over 1364-2005 (Design Subset) ( | 0:28 | Protocol and design detail: What 1800-2005 Adds Over 1364-2005 (Design Subset) (2). Packages and import: Shared types, constants, and functions across modules unique case, priority case: Standard semantics for case statements (no tool-specific attributes) Operators: inside, wildcard ==?, and other enhancements Verification: Classes, assertions (SVA), coverage, etc. (covered in later modules or... |
| 30 | Detail: What This Module Emphasizes | 0:20 | Protocol and design detail: What This Module Emphasizes. Design/RTL: logic, always_comb/always_ff, interfaces, packages, unique/priority case Not in depth: Classes, SVA, coverage, constrained random (those are verification-focused) From MODULE4 Topics Covered — protocol/design depth. |
| 31 | Detail: Relation to 1364-2005 | 0:20 | Protocol and design detail: Relation to 1364-2005. 1364-2005 (Verilog) is a subset of 1800-2005 (SystemVerilog). Valid 1364-2005 RTL is valid 1800-2005 RTL; you can migrate incrementally (e.g. wire→logic, always @*→always_comb). From MODULE4 Topics Covered — protocol/design depth. |
| 32 | Detail: logic (4-State, Single Driver) (1) | 0:28 | Protocol and design detail: logic (4-State, Single Driver) (1). logic: Replaces wire and reg when there is a single driver; can be used in both continuous and procedural assignments. Rule: A logic variable must have exactly one driver (one assign, or one always block, or one port connection). 4-state: 0, 1, X, Z (same as wire/reg). Ports: input logic, output logic; no need to choose wire vs... |
| 33 | Detail: logic (4-State, Single Driver) (2) | 0:16 | Protocol and design detail: logic (4-State, Single Driver) (2). Internal: Use logic for single-driver signals; use wire only when needed (e.g. multiple drivers, tri-state). From MODULE4 Topics Covered — protocol/design depth. |
| 34 | Detail: bit and 2-State Types (1800) | 0:24 | Protocol and design detail: bit and 2-State Types (1800). bit: 2-state (0, 1); no X or Z. Often used in testbenches and for indices. int, byte, shortint, longint: Integer types; 2-state unless declared with a 4-state base. Use in RTL: Optional (e.g. loop indices, parameters); many RTL designs use logic and integer/reg for compatibility. From MODULE4 Topics Covered — protocol/design depth. |
| 35 | Detail: always_comb | 0:28 | Protocol and design detail: always_comb. Intent: Combinational logic; no storage; outputs are a function of inputs only. Sensitivity: Automatically inferred (like always @*); must not contain latches. Tool check: Simulator/synthesis can warn if the block implies storage or is sensitive to clock edges. Prefer over always @* when the tool supports it: clearer intent and better checking. From... |
| 36 | Detail: always_ff | 0:24 | Protocol and design detail: always_ff. Intent: Sequential logic (flip-flops); use nonblocking assignment (<=) only. Sensitivity: Must list exactly one clock (and optionally async reset); no other event controls. Tool check: Can warn if blocking assignment or incomplete sensitivity is used. From MODULE4 Topics Covered — protocol/design depth. |
| 37 | Detail: always_latch | 0:24 | Protocol and design detail: always_latch. Intent: Explicit latch (level-sensitive storage). Use sparingly; usually latches are unintended. When: When a latch is truly intended (e.g. transparent latch); otherwise use always_comb or always_ff. Recommendation: Prefer always_comb or always_ff; use always_latch only when a latch is desired. From MODULE4 Topics Covered — protocol/design depth. |
| 38 | Detail: Interface Declaration | 0:20 | Protocol and design detail: Interface Declaration. Signals: Declared once inside the interface; shared by all modules that use it. modport: Defines a view (direction) for a given role (e.g. master sees data as output, slave sees data as input). From MODULE4 Topics Covered — protocol/design depth. |
| 39 | Detail: Using an Interface in a Module | 0:20 | Protocol and design detail: Using an Interface in a Module. Port: Single interface port with a modport (e.g. bus_if.master bus). Access: bus.signal_name; direction is enforced by the modport. From MODULE4 Topics Covered — protocol/design depth. |
| 40 | Detail: Top-Level Connection | 0:16 | Protocol and design detail: Top-Level Connection. One interface instance: Connects master and slave; no long port lists. From MODULE4 Topics Covered — protocol/design depth. |
| 41 | Detail: Package Declaration | 0:20 | Protocol and design detail: Package Declaration. Contents: parameter, localparam, typedef, function, task, etc. (no always blocks). Scope: Items are in namespace pkg_util:: until imported. From MODULE4 Topics Covered — protocol/design depth. |
| 42 | Detail: Import and Use | 0:20 | Protocol and design detail: Import and Use. import pkg_util::*: Import all names from the package (can cause name clashes). import pkg_util::item: Import specific items; safer when many packages are used. From MODULE4 Topics Covered — protocol/design depth. |
| 43 | Detail: unique case | 0:24 | Protocol and design detail: unique case. Semantics: At most one case item must match at runtime; if none or more than one matches, a runtime error can be reported. Use: When the design guarantees at most one match (e.g. one-hot encoding, mutually exclusive conditions). Synthesis: Can be implemented as parallel priority logic; no inferred priority order. From MODULE4 Topics Covered —... |
| 44 | Detail: priority case | 0:24 | Protocol and design detail: priority case. Semantics: The first matching item has priority; if no item matches, a runtime error can be reported (unless default is present). Use: When priority order is intended (e.g. interrupt mask). Synthesis: Can be implemented as a priority chain (if-then-else or similar). From MODULE4 Topics Covered — protocol/design depth. |
| 45 | Verification flow (reference) | 0:22 | Verification flow (reference). Stimulus, DUT response, and check points for this module. |
| 46 | 1. IEEE 1800-2005 Context (1/2) | 0:36 | 1. IEEE 1800-2005 Context (1/2). logic: Single type for single-driver nets/variables (replaces wire/reg in most RTL) 2-state types: bit, int, byte, etc. (mainly for testbenches; some RTL use) always_comb, always_ff, always_latch: Explicit procedural blocks with tool checks Interfaces and modports: Bundle signals and define direction per connection Packages and import: Shared types, constants... |
| 47 | 1. IEEE 1800-2005 Context (2/2) | 0:36 | 1. IEEE 1800-2005 Context (2/2). Operators: inside, wildcard ==?, and other enhancements Verification: Classes, assertions (SVA), coverage, etc. (covered in later modules or other courses) Design/RTL: logic, always_comb/always_ff, interfaces, packages, unique/priority case Not in depth: Classes, SVA, coverage, constrained random (those are verification-focused) 1364-2005 (Verilog) is a subset... |
| 48 | 2. logic and 2-State Types (1800) (1/2) | 0:36 | 2. logic and 2-State Types (1800) (1/2). logic: Replaces wire and reg when there is a single driver; can be used in both continuous and procedural assignments. Rule: A logic variable must have exactly one driver (one assign, or one always block, or one port connection). 4-state: 0, 1, X, Z (same as wire/reg). Ports: input logic, output logic; no need to choose wire vs reg for single-driver... |
| 49 | 2. logic and 2-State Types (1800) (2/2) | 0:20 | 2. logic and 2-State Types (1800) (2/2). int, byte, shortint, longint: Integer types; 2-state unless declared with a 4-state base. Use in RTL: Optional (e.g. loop indices, parameters); many RTL designs use logic and integer/reg for compatibility. |
| 50 | 3. always_comb, always_ff, always_latch (1800) (1/2) | 0:36 | 3. always_comb, always_ff, always_latch (1800) (1/2). Intent: Combinational logic; no storage; outputs are a function of inputs only. Sensitivity: Automatically inferred (like always @*); must not contain latches. Tool check: Simulator/synthesis can warn if the block implies storage or is sensitive to clock edges. Prefer over always @* when the tool supports it: clearer intent and better... |
| 51 | 3. always_comb, always_ff, always_latch (1800) (2/2) | 0:28 | 3. always_comb, always_ff, always_latch (1800) (2/2). Tool check: Can warn if blocking assignment or incomplete sensitivity is used. Intent: Explicit latch (level-sensitive storage). Use sparingly; usually latches are unintended. When: When a latch is truly intended (e.g. transparent latch); otherwise use always_comb or always_ff. Recommendation: Prefer always_comb or always_ff; use... |
| 52 | 4. Interfaces and Modports (1800) | 0:32 | 4. Interfaces and Modports (1800). Signals: Declared once inside the interface; shared by all modules that use it. modport: Defines a view (direction) for a given role (e.g. master sees data as output, slave sees data as input). Port: Single interface port with a modport (e.g. bus_if.master bus). Access: bus.signal_name; direction is enforced by the modport. One interface instance: Connects... |
| 53 | 5. Packages and import (1800) | 0:28 | 5. Packages and import (1800). Contents: parameter, localparam, typedef, function, task, etc. (no always blocks). Scope: Items are in namespace pkg_util:: until imported. import pkg_util::*: Import all names from the package (can cause name clashes). import pkg_util::item: Import specific items; safer when many packages are used. |
| 54 | 6. unique case and priority case (1800) | 0:36 | 6. unique case and priority case (1800). Semantics: At most one case item must match at runtime; if none or more than one matches, a runtime error can be reported. Use: When the design guarantees at most one match (e.g. one-hot encoding, mutually exclusive conditions). Synthesis: Can be implemented as parallel priority logic; no inferred priority order. Semantics: The first matching item has... |
| 55 | 7. Comparison: 1364-2005 vs 1800-2005 (Design) | 0:16 | 7. Comparison: 1364-2005 vs 1800-2005 (Design). See docs/MODULE4.md |
| 56 | Hands-on examples | 0:08 | Next section: Hands-on examples. |
| 57 | Module 4 toolchain check | 0:45 | Module 4 toolchain check. Watch the terminal output and confirm you see the expected pass message. Requires iverilog on PATH (apt install iverilog). |
| 58 | Example 1: logic for ports and internal single-driver signal | 0:16 | Example 1: logic for ports and internal single-driver signals. Single driver for logic; 4-state module4/examples/data_types/README.md |
| 59 | Demo: logic for ports and internal single-driver signals | 0:45 | Demo: logic for ports and internal single-driver signals. Watch the terminal output and confirm you see the expected pass message. |
| 60 | Example 2: All ports logic (mux, adder); no wire/reg choice | 0:16 | Example 2: All ports logic (mux, adder); no wire/reg choice. assign and always_comb can drive output logic module4/examples/logic_ports/README.md |
| 61 | Demo: All ports logic (mux, adder); no wire/reg choice | 0:45 | Demo: All ports logic (mux, adder); no wire/reg choice. Watch the terminal output and confirm you see the expected pass message. |
| 62 | Example 3: Combinational with always_comb; sequential with a | 0:16 | Example 3: Combinational with always_comb; sequential with always_ff. Explicit intent; tool checks; no latch in always_comb module4/examples/procedural/README.md |
| 63 | Demo: Combinational with always_comb; sequential with always | 0:45 | Demo: Combinational with always_comb; sequential with always_ff. Watch the terminal output and confirm you see the expected pass message. |
| 64 | Example 4: Explicit latch when intended (e.g. transparent la | 0:16 | Example 4: Explicit latch when intended (e.g. transparent latch). Use when latch is desired; prefer always_comb/always_ff otherwise module4/examples/always_latch/README.md |
| 65 | Demo: Explicit latch when intended (e.g. transparent latch) | 0:45 | Demo: Explicit latch when intended (e.g. transparent latch). Watch the terminal output and confirm you see the expected pass message. |
| 66 | Example 5: Single assign to one output; single always_ff to | 0:16 | Example 5: Single assign to one output; single always_ff to another. logic must have exactly one driver module4/examples/one_driver_logic/README.md |
| 67 | Demo: Single assign to one output; single always_ff to anoth | 0:45 | Demo: Single assign to one output; single always_ff to another. Watch the terminal output and confirm you see the expected pass message. |
| 68 | Example 6: typedef in module (e.g. byte_t = logic [7:0]) | 0:16 | Example 6: typedef in module (e.g. byte_t = logic [7:0]). User-defined types for clarity module4/examples/typedef_sv/README.md |
| 69 | Demo: typedef in module (e.g. byte_t = logic [7:0]) | 0:45 | Demo: typedef in module (e.g. byte_t = logic [7:0]). Watch the terminal output and confirm you see the expected pass message. |
| 70 | Example 7: Interface definition, modports, connection in top | 0:16 | Example 7: Interface definition, modports, connection in top. One bundle per connection; direction per modport module4/examples/interfaces/README.md |
| 71 | Demo: Interface definition, modports, connection in top | 0:45 | Demo: Interface definition, modports, connection in top. Watch the terminal output and confirm you see the expected pass message. |
| 72 | Example 8: Package with types/params/functions; import in mo | 0:16 | Example 8: Package with types/params/functions; import in modules. Namespace; wildcard vs specific import module4/examples/packages/README.md |
| 73 | Demo: Package with types/params/functions; import in modules | 0:45 | Demo: Package with types/params/functions; import in modules. Watch the terminal output and confirm you see the expected pass message. |
| 74 | Example 9: Package with typedef and min_word function; impor | 0:16 | Example 9: Package with typedef and min_word function; import in ALU. Shared types and helpers across modules module4/examples/package_typedef/README.md |
| 75 | Demo: Package with typedef and min_word function; import in | 0:45 | Demo: Package with typedef and min_word function; import in ALU. Watch the terminal output and confirm you see the expected pass message. |
| 76 | Example 10: unique case (at most one match); priority case ( | 0:16 | Example 10: unique case (at most one match); priority case (first match). Standard semantics; synthesis and simulation behavior module4/examples/case_unique_priority/README.md |
| 77 | Demo: unique case (at most one match); priority case (first | 0:45 | Demo: unique case (at most one match); priority case (first match). Watch the terminal output and confirm you see the expected pass message. |
| 78 | Example 11: Arbiter with priority case (first req wins) | 0:16 | Example 11: Arbiter with priority case (first req wins). priority case for arbiter, interrupt mask module4/examples/priority_case/README.md |
| 79 | Demo: Arbiter with priority case (first req wins) | 0:45 | Demo: Arbiter with priority case (first req wins). Watch the terminal output and confirm you see the expected pass message. |
| 80 | Example 12: Same small design in 1364-2005 style vs 1800-200 | 0:16 | Example 12: Same small design in 1364-2005 style vs 1800-2005 style. wire/reg→logic, always @*→always_comb, always @(posedge clk)→always_ff module4/examples/migration/README.md |
| 81 | Demo: Same small design in 1364-2005 style vs 1800-2005 styl | 0:45 | Demo: Same small design in 1364-2005 style vs 1800-2005 style. Watch the terminal output and confirm you see the expected pass message. |
| 82 | Common pitfalls | 0:08 | Next section: Common pitfalls. |
| 83 | Multiple Drivers on logic | 0:28 | Multiple Drivers on logic. Mistake: Driving the same logic from two assign statements or two always blocks. Reality: logic allows only one driver; multiple drivers are illegal or undefined. Correct: Use one assign or one always per logic; for multiple drivers use wire (e.g. tri-state). Why: logic is for single-driver RTL; wire is for resolved (multi-driver) nets. |
| 84 | Using always_comb with Latched Behavior | 0:28 | Using always_comb with Latched Behavior. Mistake: Incomplete branches in always_comb (e.g. no default in case) and expecting no latch. Reality: Tools may infer a latch or report a warning. Correct: Assign every output in every path, or provide default before case. Why: always_comb is intended for pure combinational logic; latches require always_latch if desired. |
| 85 | Wrong Modport or Direction | 0:28 | Wrong Modport or Direction. Mistake: Driving an input in a modport or reading an output that is not in the modport. Reality: Compiler or elaboration error; or wrong connectivity. Correct: Use the modport that matches the module role (master vs slave); check direction in the interface. Why: Modports enforce direction; using the wrong view breaks the contract. |
| 86 | Forgetting to import Package | 0:28 | Forgetting to import Package. Mistake: Using a package type or function without importing. Reality: Identifier not found; compile error. Correct: Add import pkg_name::*; or import pkg_name::item; before use. Why: Package names are in a separate namespace until imported. |
| 87 | unique case with Multiple Matches | 0:28 | unique case with Multiple Matches. Mistake: Writing unique case when two items can match (e.g. overlapping or X in select). Reality: Runtime error or undefined behavior. Correct: Use unique case only when at most one item can match; use default for catch-all; or use priority case when priority is i Why: unique case has well-defined semantics; violating them breaks verification and synthesis. |
| 88 | Practice & assessment | 0:08 | Next section: Practice & assessment. |
| 89 | What you should know (1/2) | 0:36 | By now you should be able to explain the following. ✓ Use logic for single-driver nets and variables in RTL ✓ Use always_comb for combinational logic and always_ff for sequential logic ✓ Define and use interfaces with modports for bundled connectivity ✓ Define packages and import them in modules ✓ Use unique case and priority case with correct semantics From MODULE4 Learning Outcomes. |
| 90 | What you should know (2/2) | 0:16 | By now you should be able to explain the following. ✓ Decide when to adopt 1800 design features vs stay with 1364 From MODULE4 Learning Outcomes. |
| 91 | Exercises | 0:32 | Exercises. logic and always_comb/always_ff Interface Package unique case Migration Summary |
| 92 | Exercises | 0:32 | Exercises. Multiple Drivers on logic Using always_comb with Latched Behavior Wrong Modport or Direction Forgetting to import Package unique case with Multiple Matches |
| 93 | Summary & next steps | 0:28 | In summary: Master the first SystemVerilog standard (IEEE 1800-2005) for RTL design: logic types, always_comb/always_ff, interfaces, packages, and unique/priority case. Next up: Next module in course. Master the first SystemVerilog standard (IEEE 1800-2005) for RTL design: logic types, always_comb/always_ff, interfaces, packages, and... Complete module4/CHECKLIST.md Review module4/EXAMPLES.md... |

        ## Section narration (edit for TTS)

        - **How to learn:** Skim this document — Goal, Overview, and Topics Covered set the IEEE scope for Module 4. Then Study design architecture — See how DUTs, examples, and testbenches fit together under `module4/`. Then Work through labs in order — Open `module4/EXAMPLES.md` and run each `make clean && make run` from the repo root. Then Run the full module script — `./scripts/module4.sh` from the repo root to simulate all examples and tests..
- **Design architecture (Repository layout (module4/), SystemVerilog design layering, File and compilation units):** Walk through the block diagram, then relate each block to files under module4/examples/.
- **Verification (Running SystemVerilog labs, What to check in simulation):** Explain what stimulus is applied, what is checked, and what is intentionally out of scope.
- **Syllabus:** Cover 8 topic section(s) — pause on protocol timing and signals.
- **Before exercises:** Ask learners to recall the learning outcomes slide; they should explain each bullet in their own words.
- **Hands-on:** Run module4/EXAMPLES.md labs; narrate expected PASS lines.

        ## Notes

        - Slides from **Before You Start**, **Design Architecture**, **Verification & Testing Methods**, **Topics Covered**, **EXAMPLES.md**, and **Learning Outcomes**.
        - Full detail: `docs/MODULE4.md` and `module4/EXAMPLES.md`.
        - Regenerate: `regenerate_course_outlines.sh <course_root> --module 4`
