# Module 6 Examples

Hands-on **Icarus Verilog** demos for Module 6. Run from the course repo root unless noted.

---

## 1. Verilog vs SystemVerilog Subset (`subsets/`)

Build and simulate: `module6/examples/subsets/`.

**Try these** (from repo root):

```bash
cd module6/examples/subsets
make clean && make run
```

---

## 2. Design Constructs Recap (`design_recap/`)

Build and simulate: `module6/examples/design_recap/`.

**Try these** (from repo root):

```bash
cd module6/examples/design_recap
make clean && make run
```

---

## 3. Immediate Assertion in RTL (`assertions/`)

Build and simulate: `module6/examples/assertions/`.

**Try these** (from repo root):

```bash
cd module6/examples/assertions
make clean && make run
```

---

## 4. Version Comparison (`version_summary/`)

Build and simulate: `module6/examples/version_summary/`.

**Try these** (from repo root):

```bash
cd module6/examples/version_summary
make clean && make run
```

---

## 5. Migration (`migration/`)

Build and simulate: `module6/examples/migration/`.

**Try these** (from repo root):

```bash
cd module6/examples/migration
make clean && make run
```

---

## 6. Logic Single Driver (`logic_single_driver/`)

Build and simulate: `module6/examples/logic_single_driver/`.

**Try these** (from repo root):

```bash
cd module6/examples/logic_single_driver
make clean && make run
```

---

## 7. Priority Case (`priority_case/`)

Build and simulate: `module6/examples/priority_case/`.

**Try these** (from repo root):

```bash
cd module6/examples/priority_case
make clean && make run
```

---

## 8. Package and Import (`package_import/`)

Build and simulate: `module6/examples/package_import/`.

**Try these** (from repo root):

```bash
cd module6/examples/package_import
make clean && make run
```

---

## 9. Unique Case (`unique_case/`)

Build and simulate: `module6/examples/unique_case/`.

**Try these** (from repo root):

```bash
cd module6/examples/unique_case
make clean && make run
```

---
